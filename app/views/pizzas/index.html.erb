<p style="color: green"><%= notice %></p>

<% content_for :title, "As pizzas, neh?" %>

<h1>Pizzas, p-i-z-z-a-s</h1>

<div id="pizzas">
  <% @pizzas.each do |pizza| %>
    <%= render pizza %>
    <p>
      <%= link_to "Show this pizza", pizza %>
    </p>
  <% end %>
</div>

<%= link_to "New pizza", new_pizza_path %>
